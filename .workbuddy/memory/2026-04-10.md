## 2026-04-10

### 日历页面布局修复（三轮迭代完成）
- **第一轮**：发现所有日历分栏CSS都写在 @media(≥768px)内，移动端完全缺失→添加移动端基础样式
- **第二轮**：发现3套冲突日历grid/flex样式→统一为grid声明
- **第三轮（最终）**：删除整块重复的移动端日历样式（~100行），保留唯一mobile-first基础CSS。**关键bug**：`.cal-left .calendar-grid{flex:1}` 覆盖了grid容器的display属性导致网格崩溃→改为 `flex:1 1 auto;min-height:0`
- **最终架构**：一套mobile-first基础CSS + 桌面端@media增量覆盖，无重复规则
- **教训**：修改已有CSS必须全局搜索所有同名选择器；flex:1对grid容器是危险的覆盖
